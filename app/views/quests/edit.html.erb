<div class="form-page">
  <div class="form-page-inner">
    <h1 class="form-page-title">Editar Jornada</h1>
    <p class="form-page-subtitle">Você pode editar o título e a descrição.</p>

    <%= form_with model: @quest do |f| %>
      <%= render "shared/form_errors", record: @quest %>

      <div class="form-section">
        <%= f.label :title, "Qual é a jornada?", class: "form-label" %>
        <%= f.text_field :title, class: "form-input on-card" %>
      </div>

      <div class="form-section">
        <%= f.label :description, "Por que essa jornada importa?", class: "form-label" %>
        <%= f.text_area :description, class: "form-input form-textarea on-card", rows: 3 %>
      </div>

      <%= f.submit "Salvar alterações", class: "form-submit" %>
    <% end %>

    <%= link_to "← Voltar", quest_path(@quest), class: "form-back-link" %>
  </div>
</div>
